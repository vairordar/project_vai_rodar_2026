{
  "name": "vai-rodar",
  "version": "1.0.0",
  "description": "Vai Rodar — plataforma automotiva brasileira",
  "private": true,
  "scripts": {
    "build": "node scripts/build-dist.js"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18"
  }
}
