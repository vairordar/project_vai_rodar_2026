# User app anterior ao MVP V.2

Backup do `apps/user-app/index.html` que estava em producao no commit
`b96a791` antes da publicacao do frontend MVP V.2 em 13/07/2026.

Os arquivos compartilhados (`assets`, `legal`, `manifest.json`,
`service-worker.js`, `robots.txt` e `sitemap.xml`) permanecem na aplicacao
principal e nao foram duplicados porque nao foram substituidos pelo MVP.
